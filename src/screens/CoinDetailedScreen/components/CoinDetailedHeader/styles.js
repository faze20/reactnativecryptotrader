import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    headerContainer:{
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-between'
    },
    ticketContainer:{
        flexDirection: 'row',
        alignItems: 'center'
    },
    rankcontainer:{
        backgroundColor: '585846',
        paddingHorizontal: 3,
    }
})

export default styles ;